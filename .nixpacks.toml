[variables]
NODE_VERSION = "18"

[phases.setup]
nixPkgs = ["nodejs_18", "npm"]

[phases.install]
cmds = [
  "npm ci"
]

[phases.build]
cmds = [
  "npm run build:client",
  "ls -la dist/",
  "ls -la dist/client/ || echo 'Client build failed'"
]

[phases.start]
cmd = "node railway-deploy.js"
